{
  "name": "cygha-live",
  "version": "1.0.0",
  "description": "E2E training",
  "main": "index.html",
  "scripts": {
    "cy:local:open": "cypress open --e2e --browser chrome --config baseUrl=http://127.0.0.1:8082/",
    "cy:local:run": "cypress run --e2e --browser chrome --config baseUrl=http://127.0.0.1:8082/",
    "cy:ci:run": "cypress run --e2e --browser chrome --config baseUrl=https://automationtester-academy.github.io/cy-gha-live/"
  },
  "author": "Sylvain Viole",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.7.0",
    "http-server": "^14.1.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.2",
    "mochawesome-report-generator": "^6.2.0"
  }
}
